import './repeat';

// will be transformed
let a = 2 ** 2;
let fn = item => item == 2;

// will not
[1, 2, 3].findIndex(fn);
'abc'.includes('def');
Object.assign({}, { a: 1 });

let promise = new Promise

// helpers
let name = 'js';
let language = {
  [name]: 'es6'
};

// generator
function* tttest() {}
